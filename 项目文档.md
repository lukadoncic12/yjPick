# 项目文档

## 1. 项目概述

HmDianPing是一个基于Spring Boot的点评系统，类似于大众点评，主要功能包括商户管理、用户管理、探店博客、评论、关注、优惠券和秒杀等功能。

### 1.1 核心功能
- 用户模块：注册、登录、用户信息管理
- 商户模块：商户信息管理、商户类型管理
- 博客模块：发布探店博客、评论、点赞
- 关注模块：用户之间的关注关系管理
- 优惠券模块：普通优惠券和秒杀优惠券管理
- 订单模块：优惠券订单管理，特别是秒杀订单的处理

### 1.2 技术亮点
- 使用Redis实现缓存，提高系统性能
- 使用Redisson实现分布式锁，确保并发安全
- 使用RabbitMQ处理异步任务，提高系统响应速度
- 使用Lua脚本实现秒杀功能，确保高并发下的数据一致性
- 分层架构设计，代码结构清晰

## 2. 技术栈

| 类别 | 技术 | 版本 | 说明 |
| :--- | :--- | :--- | :--- |
| 基础框架 | Spring Boot | 2.3.12 | 应用基础框架 |
| 数据库 | MySQL | 8.0.28 | 数据存储 |
| 缓存 | Redis | - | 缓存、分布式锁 |
| 消息队列 | RabbitMQ | - | 异步任务处理 |
| ORM | MyBatis-Plus | 3.4.3 | 数据库访问 |
| 工具库 | Hutool | 5.7.17 | 工具类库 |
| 分布式锁 | Redisson | 3.13.6 | 分布式锁实现 |
| 其他 | AspectJ | - | 切面编程 |

## 3. 项目结构

```
src/
├── main/
│   ├── java/com/hmdp/
│   │   ├── config/         # 配置类
│   │   ├── controller/     # 控制器
│   │   ├── dto/            # 数据传输对象
│   │   ├── entity/         # 实体类
│   │   ├── interceptor/    # 拦截器
│   │   ├── listener/       # 监听器
│   │   ├── mapper/         # Mapper接口
│   │   ├── service/        # 服务接口及实现
│   │   │   └── impl/       # 服务实现
│   │   ├── utils/          # 工具类
│   │   └── HmDianPingApplication.java # 应用入口
│   └── resources/
│       ├── db/             # 数据库脚本
│       ├── mapper/         # MyBatis XML映射文件
│       ├── application.yaml # 应用配置
│       ├── seckill.lua     # 秒杀Lua脚本
│       └── unLock.lua      # 解锁Lua脚本
└── test/                   # 测试类
```

### 3.1 主要包结构说明

| 包名 | 职责 | 主要文件 |
| :--- | :--- | :--- |
| config | 应用配置 | MvcConfig.java, RedissonConfig.java, QueueConfig.java |
| controller | 请求处理 | UserController.java, ShopController.java, BlogController.java, VoucherController.java |
| dto | 数据传输对象 | LoginFormDTO.java, UserDTO.java, Result.java |
| entity | 实体类 | User.java, Shop.java, Blog.java, Voucher.java |
| interceptor | 拦截器 | LoginInterceptor.java, RefreshTokenInterceptor.java |
| listener | 监听器 | SeckillVoucherListener.java |
| mapper | 数据访问 | UserMapper.java, ShopMapper.java, BlogMapper.java |
| service | 业务逻辑 | UserService.java, ShopService.java, BlogService.java |
| utils | 工具类 | CacheClient.java, RedisIdWorker.java, SimpleRedisLock.java |

## 4. 核心功能模块

### 4.1 用户模块

#### 4.1.1 功能说明
- 用户注册：通过手机号注册
- 用户登录：手机号验证码登录
- 用户信息管理：获取和更新用户信息
- 令牌刷新：自动刷新用户令牌

#### 4.1.2 关键类
- `UserController`：处理用户相关请求
- `UserServiceImpl`：实现用户相关业务逻辑
- `LoginInterceptor`：登录拦截器
- `RefreshTokenInterceptor`：令牌刷新拦截器

### 4.2 商户模块

#### 4.2.1 功能说明
- 商户信息管理：获取商户详情、更新商户信息
- 商户类型管理：获取商户类型列表
- 商户缓存：使用Redis缓存商户信息，提高访问速度

#### 4.2.2 关键类
- `ShopController`：处理商户相关请求
- `ShopServiceImpl`：实现商户相关业务逻辑
- `ShopTypeController`：处理商户类型相关请求
- `CacheClient`：缓存客户端工具类

### 4.3 博客模块

#### 4.3.1 功能说明
- 发布博客：用户发布探店博客
- 博客列表：获取博客列表，支持分页
- 博客详情：获取博客详情
- 点赞功能：用户对博客进行点赞
- 评论功能：用户对博客进行评论

#### 4.3.2 关键类
- `BlogController`：处理博客相关请求
- `BlogServiceImpl`：实现博客相关业务逻辑
- `BlogCommentsController`：处理评论相关请求
- `BlogCommentsServiceImpl`：实现评论相关业务逻辑

### 4.4 关注模块

#### 4.4.1 功能说明
- 关注用户：用户关注其他用户
- 取消关注：用户取消关注
- 共同关注：获取两个用户的共同关注
- 关注列表：获取用户的关注列表

#### 4.4.2 关键类
- `FollowController`：处理关注相关请求
- `FollowServiceImpl`：实现关注相关业务逻辑

### 4.5 优惠券模块

#### 4.5.1 功能说明
- 优惠券管理：创建和管理优惠券
- 秒杀优惠券：创建和管理秒杀优惠券
- 优惠券列表：获取用户可领取的优惠券

#### 4.5.2 关键类
- `VoucherController`：处理优惠券相关请求
- `VoucherServiceImpl`：实现优惠券相关业务逻辑
- `SeckillVoucherServiceImpl`：实现秒杀优惠券相关业务逻辑

### 4.6 订单模块

#### 4.6.1 功能说明
- 优惠券订单：用户领取优惠券
- 秒杀订单：用户抢购秒杀优惠券
- 订单处理：异步处理订单

#### 4.6.2 关键类
- `VoucherOrderController`：处理订单相关请求
- `VoucherOrderServiceImpl`：实现订单相关业务逻辑
- `SeckillVoucherListener`：处理秒杀订单的监听器

## 5. 数据库设计

### 5.1 主要表结构

#### 5.1.1 用户表（tb_user）
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| id | bigint(20) | 主键 |
| phone | varchar(11) | 手机号码 |
| password | varchar(128) | 密码，加密存储 |
| nick_name | varchar(32) | 昵称 |
| icon | varchar(255) | 头像 |
| create_time | timestamp | 创建时间 |
| update_time | timestamp | 更新时间 |

#### 5.1.2 商户表（tb_shop）
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| id | bigint(20) | 主键 |
| name | varchar(128) | 商户名称 |
| type_id | bigint(20) | 商户类型ID |
| images | varchar(1024) | 商户图片 |
| area | varchar(128) | 商圈 |
| address | varchar(255) | 地址 |
| x | double | 经度 |
| y | double | 纬度 |
| avg_price | bigint(10) | 均价 |
| sold | int(10) | 销量 |
| comments | int(10) | 评论数量 |
| score | int(2) | 评分 |
| open_hours | varchar(32) | 营业时间 |
| create_time | timestamp | 创建时间 |
| update_time | timestamp | 更新时间 |

#### 5.1.3 博客表（tb_blog）
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| id | bigint(20) | 主键 |
| shop_id | bigint(20) | 商户ID |
| user_id | bigint(20) | 用户ID |
| title | varchar(255) | 标题 |
| images | varchar(2048) | 图片 |
| content | varchar(2048) | 内容 |
| liked | int(8) | 点赞数量 |
| comments | int(8) | 评论数量 |
| create_time | timestamp | 创建时间 |
| update_time | timestamp | 更新时间 |

#### 5.1.4 优惠券表（tb_voucher）
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| id | bigint(20) | 主键 |
| shop_id | bigint(20) | 商户ID |
| title | varchar(128) | 优惠券标题 |
| sub_title | varchar(256) | 优惠券副标题 |
| rules | varchar(1024) | 使用规则 |
| pay_value | bigint(10) | 支付金额 |
| actual_value | bigint(10) | 实际金额 |
| type | int(1) | 优惠券类型 |
| status | int(1) | 状态 |
| create_time | timestamp | 创建时间 |
| update_time | timestamp | 更新时间 |

#### 5.1.5 秒杀优惠券表（tb_seckill_voucher）
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| voucher_id | bigint(20) | 优惠券ID |
| stock | int(8) | 库存 |
| create_time | timestamp | 创建时间 |
| begin_time | timestamp | 开始时间 |
| end_time | timestamp | 结束时间 |
| update_time | timestamp | 更新时间 |

#### 5.1.6 关注表（tb_follow）
| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| id | bigint(20) | 主键 |
| user_id | bigint(20) | 用户ID |
| follow_user_id | bigint(20) | 关注的用户ID |
| create_time | timestamp | 创建时间 |

## 6. 核心API接口

### 6.1 用户相关接口

| API路径 | 方法 | 功能 |
| :--- | :--- | :--- |
| /user/code | POST | 发送验证码 |
| /user/login | POST | 用户登录 |
| /user/logout | POST | 用户退出 |
| /user/me | GET | 获取当前用户信息 |
| /user/info/{id} | GET | 获取指定用户信息 |
| /user/update | PUT | 更新用户信息 |

### 6.2 商户相关接口

| API路径 | 方法 | 功能 |
| :--- | :--- | :--- |
| /shop/type | GET | 获取商户类型列表 |
| /shop/{id} | GET | 获取商户详情 |
| /shop/update | PUT | 更新商户信息 |
| /shop/list | GET | 获取商户列表（分页） |

### 6.3 博客相关接口

| API路径 | 方法 | 功能 |
| :--- | :--- | :--- |
| /blog/hot | GET | 获取热门博客 |
| /blog/{id} | GET | 获取博客详情 |
| /blog | POST | 发布博客 |
| /blog/my | GET | 获取当前用户的博客 |
| /blog/update | PUT | 更新博客 |
| /blog/delete | DELETE | 删除博客 |
| /blog/like/{id} | GET | 点赞/取消点赞 |

### 6.4 评论相关接口

| API路径 | 方法 | 功能 |
| :--- | :--- | :--- |
| /blog/comments | POST | 发表评论 |
| /blog/comments/{id} | GET | 获取博客评论 |

### 6.5 关注相关接口

| API路径 | 方法 | 功能 |
| :--- | :--- | :--- |
| /follow | POST | 关注用户 |
| /follow/{id} | DELETE | 取消关注 |
| /follow/common/{id} | GET | 获取共同关注 |
| /follow/followers/{id} | GET | 获取粉丝列表 |
| /follow/followings/{id} | GET | 获取关注列表 |

### 6.6 优惠券相关接口

| API路径 | 方法 | 功能 |
| :--- | :--- | :--- |
| /voucher/list/{shopId} | GET | 获取商户优惠券列表 |
| /voucher/seckill | POST | 创建秒杀优惠券 |
| /voucher-order/seckill/{id} | POST | 秒杀优惠券 |

## 7. 核心技术实现

### 7.1 缓存实现

使用Redis实现缓存，主要用于缓存商户信息、热点数据等，提高系统性能。

```java
// CacheClient.java
public <T> void set(String key, T value, Long time, TimeUnit unit) {
    stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(value), time, unit);
}

public <T> T get(String key, Class<T> type) {
    String json = stringRedisTemplate.opsForValue().get(key);
    if (StrUtil.isBlank(json)) {
        return null;
    }
    return JSONUtil.toBean(json, type);
}
```

### 7.2 分布式锁实现

使用Redisson实现分布式锁，确保并发操作的安全性，特别是在秒杀场景中。

```java
// SimpleRedisLock.java
@Override
public boolean tryLock(long timeoutSec) {
    String threadId = Thread.currentThread().getId() + UUID.randomUUID().toString();
    Boolean success = stringRedisTemplate.opsForValue()
            .setIfAbsent(KEY_PREFIX + name, threadId, timeoutSec, TimeUnit.SECONDS);
    return Boolean.TRUE.equals(success);
}

@Override
public void unlock() {
    stringRedisTemplate.delete(KEY_PREFIX + name);
}
```

### 7.3 秒杀功能实现

使用Lua脚本实现秒杀功能，确保高并发下的数据一致性。

```lua
-- seckill.lua
local voucherId = ARGV[1]
local userId = ARGV[2]
local orderId = ARGV[3]

local stockKey = 'seckill:stock:' .. voucherId
local orderKey = 'seckill:order:' .. voucherId

if(tonumber(redis.call('get', stockKey)) <= 0) then
    return 1
end

if(redis.call('sismember', orderKey, userId) == 1) then
    return 2
end

redis.call('incrby', stockKey, -1)
redis.call('sadd', orderKey, userId)

return 0
```

### 7.4 异步任务处理

使用RabbitMQ处理异步任务，如秒杀订单的处理，提高系统响应速度。

```java
// VoucherOrderServiceImpl.java
@Override
public Result seckillVoucher(Long voucherId) {
    // 1.执行秒杀lua脚本
    Long result = stringRedisTemplate.execute(
            SECKILL_SCRIPT,
            Collections.emptyList(),
            voucherId.toString(),
            userId.toString(),
            orderId.toString()
    );
    int r = result.intValue();
    // 2.判断结果是否为0
    if (r != 0) {
        // 2.1.不为0，代表没有购买资格
        return Result.fail(r == 1 ? "库存不足" : "不能重复下单");
    }
    // 3.创建订单
    VoucherOrder voucherOrder = new VoucherOrder();
    // 3.1.订单id
    long orderId = redisIdWorker.nextId("order");
    voucherOrder.setId(orderId);
    // 3.2.用户id
    voucherOrder.setUserId(userId);
    // 3.3.代金券id
    voucherOrder.setVoucherId(voucherId);
    // 3.4.放入阻塞队列
    orderTasks.add(voucherOrder);
    // 4.返回订单id
    return Result.ok(orderId);
}
```

## 8. 部署与运行

### 8.1 环境要求

- JDK 1.8+
- MySQL 5.7+
- Redis 5.0+
- RabbitMQ 3.7+

### 8.2 运行步骤

1. 克隆项目代码
2. 创建数据库并导入`src/main/resources/db/hmdp.sql`脚本
3. 修改`src/main/resources/application.yaml`中的数据库、Redis和RabbitMQ配置
4. 运行`HmDianPingApplication.java`启动应用
5. 访问`http://localhost:8081`即可

### 8.3 测试账户

- 手机号：13686869696
- 密码：无（验证码登录）

## 9. 总结

HmDianPing是一个功能完善的点评系统，采用了Spring Boot、MySQL、Redis、RabbitMQ等技术栈，实现了用户管理、商户管理、博客管理、关注管理、优惠券管理和订单管理等核心功能。系统设计合理，代码结构清晰，具有良好的扩展性和可维护性。

通过使用Redis缓存、分布式锁和消息队列等技术，系统在性能和并发处理方面表现出色，特别是在秒杀场景中，通过Lua脚本确保了数据的一致性和系统的稳定性。

该项目为类似点评系统的开发提供了一个良好的参考，也可以作为学习Spring Boot、Redis、RabbitMQ等技术的实践项目。